# Raspberry Pi optimized requirements
# Install heavy system packages via apt:
#   sudo apt install -y python3-numpy python3-opencv python3-pil python3-scipy

# Flask and Extensions
Flask==3.0.0
Flask-CORS==4.0.0
Flask-SocketIO==5.3.5
python-socketio==5.10.0

# MQTT
paho-mqtt==1.6.1

# Database (SQLite)
peewee==3.17.0

# Object Detection (YOLO)
# Uncomment only if you have enough resources and need YOLO on Pi
# ultralytics==8.1.20

# Environment Variables
python-dotenv==1.0.0

# Async Support
eventlet==0.35.2

# Utilities
python-dateutil==2.8.2
requests==2.31.0

# Trilateration & Math
# Install via apt on Pi to avoid build errors:
#   sudo apt install -y python3-scipy

# Raspberry Pi GPIO (for DHT11)
# Adafruit-DHT==1.4.0  # Uncomment on Raspberry Pi
# RPi.GPIO==0.7.1  # Uncomment on Raspberry Pi
